@using Model.EF
@model SanPham
@{
    ViewBag.Title = "XemChiTiet";
    Layout = "~/Views/Shared/_ProductLayout.cshtml";
}

<div class="col-md-9 product-price1">
    <div class="col-md-5 single-top">
        <div class="flexslider">
            <ul class="slides">
                <li data-thumb="@Url.Content(Model.HinhAnh)">
                    <img src="@Url.Content(Model.HinhAnh)" />
                </li>
                <li data-thumb="@Url.Content(Model.HinhAnh1)">
                    <img src="@Url.Content(Model.HinhAnh1)" />
                </li>
                <li data-thumb="@Url.Content(Model.HinhAnh2)">
                    <img src="@Url.Content(Model.HinhAnh2)" />
                </li>
                <li data-thumb="@Url.Content(Model.HinhAnh3)">
                    <img src="@Url.Content(Model.HinhAnh3)" />
                </li>
                @*<li data-thumb="@Url.Content("~/Content/client/imgsp/"+Model.HinhAnh4)">
                    <img src="@Url.Content("~/Content/client/imgsp/"+Model.HinhAnh4)" />
                </li>*@
              
            </ul>
        </div>
        <!-- FlexSlider -->
        <script defer src="~/Content/client/js/jquery.flexslider.js"></script>
        <link rel="stylesheet" href="~/Content/client/css/flexslider.css" type="text/css" media="screen" />

        <script>
// Can also be used with $(document).ready()
$(window).load(function() {
  $('.flexslider').flexslider({
    animation: "slide",
    controlNav: "thumbnails"
  });
});
        </script>
    </div>
    <div class="col-md-7 single-top-in simpleCart_shelfItem">
        <div class="single-para ">
            <h4>@Model.TenSP</h4>
            <div class="star-on">
                <ul class="star-footer">
                    <li><a href="#"><i> </i></a></li>
                    <li><a href="#"><i> </i></a></li>
                    <li><a href="#"><i> </i></a></li>
                    <li><a href="#"><i> </i></a></li>
                    <li><a href="#"><i> </i></a></li>
                </ul>
                <div class="review">
                    <a href="#"></a>

                </div>
                <div class="clearfix"> </div>
            </div>

            <h5 class="item_price"> @Model.DonGia.Value.ToString("#,##")VNĐ</h5>
            <p>
               @Html.Raw(Model.CauHinh)
            </p>
           
         
            <a href="@Url.Action("ThemGioHang","GioHang",new { @MaSP=Model.MaSP,@strURL=Request.Url.ToString()})" class="add-cart item_add">Thêm giỏ hàng</a>
            <a href="@Url.Action("XemGioHang","GioHang")" class="add-cart item_add">Xem giỏ hàng</a>
        </div>
    </div>
    <div class="clearfix"> </div>
    <h3 style="font-size: 20px;margin-top: 20px; font-weight: bold;">Thông tin sản phẩm</h3>
    @Html.Raw(Model.MoTa)
    <!---->
  
    
</div> 

